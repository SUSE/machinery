Editing Extensions
==================

This directory is meant to store add-on extensions which improves
the usability of machinery with regards to all tasks a user does
while working with the tool or the data it creates. The following
list shows which editing extensions are currently supported:

- machinery command completion for bash
